//вызов данной функции скрывает или показывает элемент
const showMenu = function (className) {
    const el = document.getElementById(className);
    el.classList.toggle("show-element");
}

// document.get

